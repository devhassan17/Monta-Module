<odoo>
    <record id="monta_res_config_settings_view" model="ir.ui.view">
        <field name="name">monta.res.config.settings.form</field>
        <field name="model">res.config.settings</field>
        <field name="arch" type="xml">
            <form string="Monta Settings">
                <sheet>
                    <group string="Monta Integration">
                        <field name="monta_base_url"/>
                        <field name="monta_username" password="True"/>
                        <field name="monta_password" password="True"/>
                        <field name="monta_client_id"/>
                        <field name="monta_client_secret" password="True"/>
                        <field name="monta_timeout"/>
                        <field name="monta_inbound_enabled"/>
                    </group>
                </sheet>
                <footer>
                    <button string="Save" type="object" name="execute" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_monta_settings" model="ir.actions.act_window">
        <field name="name">Monta Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="monta_res_config_settings_view"/>
        <field name="target">new</field>
        <field name="context">{}</field>
    </record>

    <!-- Sales > Configuration > Monta Settings -->
    <menuitem id="menu_monta_settings"
              name="Monta Settings"
              parent="sale.menu_sale_config"
              action="action_monta_settings"
              sequence="95"/>
</odoo>
